# Test Profile Configuration
# Disables Flyway for tests since we use Hibernate's create-drop for test isolation

# Disable Flyway in tests
spring.flyway.enabled=false

# Use H2 or PostgreSQL with create-drop for tests
spring.jpa.hibernate.ddl-auto=create-drop
